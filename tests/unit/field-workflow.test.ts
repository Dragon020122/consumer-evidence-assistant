import{describe,expect,it}from"vitest";import{normalizeField}from"@/lib/field-state";import{deriveWorkflow}from"@/lib/workflow";
describe("字段状态与工作流",()=>{it("兼容旧三态并区分填写与证据",()=>{expect(normalizeField({value:"虚构商家",state:"CONFIRMED"})).toMatchObject({state:"USER_CONFIRMED",evidenceSupport:"USER_STATEMENT"});expect(normalizeField({value:null,state:"PENDING"}).state).toBe("EMPTY");expect(normalizeField({value:null,state:"UNKNOWN"}).state).toBe("UNABLE_TO_CONFIRM")});it("清空字段恢复待补充",()=>{expect(normalizeField({value:null,state:"EMPTY",evidenceSupport:"NONE"})).toEqual({value:null,state:"EMPTY",evidenceSupport:"NONE"})});it("材料不齐时步骤为需补充而不是硬阻断",()=>{const steps=deriveWorkflow("case",{fieldCount:11,confirmedFieldCount:11,fieldConflictCount:0,evidenceCount:1,evidenceCategories:["PAYMENT_ORDER"],extractionCount:1,pendingExtractionCount:1,timelineCount:0,timelineConfirmed:false,matrixCount:0,priorityGapCount:3,exportCount:0});expect(steps[2]).toMatchObject({status:"NEEDS_INPUT"});expect(steps[2].note).toContain("可暂时继续")})});
