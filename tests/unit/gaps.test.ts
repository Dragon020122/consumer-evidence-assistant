import{describe,expect,it}from"vitest";import{detectMaterialGaps}from"@/lib/gaps";
describe("材料缺口规则",()=>{it("识别付款、合同、沟通和主体缺口",()=>{const gaps=detectMaterialGaps([],[],[],{merchantLegalName:{value:null,state:"UNKNOWN"}});expect(gaps.map(x=>x.code)).toEqual(expect.arrayContaining(["PAYMENT_MISSING","CONTRACT_MISSING","REFUND_COMMUNICATION_MISSING","MERCHANT_UNCLEAR"]))});it("多金额只提示核对，不生成法律结论",()=>{const rows=[100,200].map((value,index)=>({id:String(index),caseId:"c",evidenceId:"e",fieldName:"amount",originalValueJson:JSON.stringify(value),confirmedValueJson:JSON.stringify(value),sourceLocator:"x",confidence:1,needsReview:0,state:"CONFIRMED",confirmedBy:"u",confirmedAt:"x",createdAt:"x"}));const gaps=detectMaterialGaps([],rows,[],{merchantLegalName:{value:"虚构商家",state:"CONFIRMED"}});expect(gaps.find(x=>x.code==="AMOUNT_CONFLICT")?.recommendation).toContain("逐份核对")})});

