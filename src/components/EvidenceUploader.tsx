"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export function EvidenceUploader({caseId}:{caseId:string}){const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMessage("正在校验并保存，请勿关闭页面…");const form=new FormData(e.currentTarget);const response=await fetch(`/api/cases/${caseId}/evidence`,{method:"POST",body:form});const data=await response.json();setLoading(false);if(!response.ok)return setMessage(data.error?.message??"上传失败，可重试");setMessage("上传完成，文件已保存在私有目录。");e.currentTarget.reset();router.refresh();}
return <form className="form upload-box" onSubmit={submit}><label>材料分类<select name="category"><option value="PAYMENT_ORDER">付款与订单</option><option value="CONTRACT">合同与服务约定</option><option value="PROMISE">宣传与商家承诺</option><option value="REFUND_COMMUNICATION">退款沟通</option><option value="SERVICE_USAGE">服务使用记录</option><option value="MERCHANT_STATUS">商家经营状态</option><option value="USER_STATEMENT">用户补充说明</option><option value="OTHER">其他材料</option></select></label><label>选择文件<input name="file" type="file" accept=".jpg,.jpeg,.png,.webp,.pdf,.txt" required/></label><button className="button" disabled={loading}>{loading?"处理中…":"安全上传"}</button>{message&&<p aria-live="polite" className="muted">{message}</p>}</form>}

