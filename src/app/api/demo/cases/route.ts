import{z}from"zod";import{getEnv}from"@/lib/env";import{errorResponse}from"@/lib/errors";import{assertSameOrigin}from"@/lib/security";import{requireSession}from"@/server/auth";import{createOrRestoreDemoCase,resetMyDemoCase}from"@/server/demo";
const schema=z.object({action:z.enum(["create","reset"]).default("create")});export async function POST(request:Request){try{assertSameOrigin(request,getEnv().APP_URL);const actor=await requireSession(["USER"]);const{action}=schema.parse(await request.json());return Response.json({case:action==="reset"?await resetMyDemoCase(actor):createOrRestoreDemoCase(actor)});}catch(error){return errorResponse(error)}}
