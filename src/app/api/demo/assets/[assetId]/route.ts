import{readFile}from"node:fs/promises";import{notFound}from"next/navigation";import{assertDemoMode,demoAssetPath,demoAssets}from"@/lib/demo";import{requireSession}from"@/server/auth";
export const runtime="nodejs";export async function GET(_:Request,{params}:{params:Promise<{assetId:string}>}){assertDemoMode();await requireSession(["USER"]);const{assetId}=await params;const asset=demoAssets.find(x=>x.id===assetId);if(!asset)notFound();const bytes=await readFile(demoAssetPath(asset.fileName));return new Response(new Uint8Array(bytes),{headers:{"Content-Type":asset.mimeType,"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}
