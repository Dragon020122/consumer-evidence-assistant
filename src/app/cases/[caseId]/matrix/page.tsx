import{notFound}from"next/navigation";import{MatrixPanel}from"@/components/MatrixPanel";import{requirePageRole}from"@/server/page-access";import{getCaseForActor}from"@/server/cases";import{listMatrix}from"@/server/matrix";
export const dynamic="force-dynamic";export default async function MatrixPage({params}:{params:Promise<{caseId:string}>}){const{caseId}=await params;const actor=await requirePageRole(["USER","REVIEWER","ADMIN"],`/cases/${caseId}/matrix`);try{getCaseForActor(actor,caseId)}catch{notFound()}return <div><div className="eyebrow">步骤 6 / 9</div><h1 className="page-title">事实—证据矩阵</h1><p className="lead">矩阵说明材料支持程度和缺口，不对事实作法律认定。</p><MatrixPanel caseId={caseId} initial={listMatrix(actor,caseId)} readOnly={actor.role==="REVIEWER"}/></div>}
