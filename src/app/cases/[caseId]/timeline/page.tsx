import { notFound } from "next/navigation";import { requirePageRole } from "@/server/page-access";import { getCaseForActor } from "@/server/cases";import { listTimeline } from "@/server/timeline";import { TimelineReview } from "@/components/TimelineReview";
export const dynamic="force-dynamic";export default async function TimelinePage({params}:{params:Promise<{caseId:string}>}){const{caseId}=await params;const user=await requirePageRole(["USER","REVIEWER","ADMIN"],`/cases/${caseId}/timeline`);try{getCaseForActor(user,caseId)}catch{notFound()}return <div><div className="eyebrow">步骤 5 / 9</div><h1 className="page-title">核对事件时间线</h1><p className="lead">证据支持事件可返回原文件；没有客观证据的内容必须标为“用户陈述”。确认后才可生成最终材料。</p><TimelineReview caseId={caseId} initial={listTimeline(user,caseId)} readOnly={user.role==="REVIEWER"}/></div>}
